fragment CollectionFragment on Collection {
  id
  name
  series
  sticker
  condition
  remarks
  stocks
  price
  negotiable
  photo {
    url
  }
}

query collections {
  collections {
    ...CollectionFragment
  }
}
