service: graphql-lambda

provider:
  name: aws
  runtime: nodejs20.x
  region: ap-southeast-1

functions:
  graphql:
    handler: src/handler.graphqlHandler
    events:
      - http:
          path: graphql
          method: post
      - http:
          path: graphql
          method: get

plugins:
  - serverless-offline
